# 维度四 · DeepGEO 自动查可见度（产品定稿）

> 产品经理 · 2026-09-15  
> 老板要求：诊断维度四用 **DeepGEO（已登录）** 自动查，**不要再问老板查什么**；从当前项目推词、查询、回填九格。后续诊断**默认**走这条。

## 目标
> **实现口径（v1.4）**：live 主路径为网页 inclusionQuery 自动化 → `applyVisGrid(provider=deepgeo_web)`。Open API 仅钱包有余额且 `DEEPGEO_USE_OPEN_API=1` 时可选。

新建/进行中的诊断，维度四（GEO 可见度）默认：
1. 从**当前项目**推导三类词（决策 / 场景 / 对比），**不测品牌词**
2. 用 DeepGEO 已登录会话对三平台（豆包 / DeepSeek / 通义千问）查询
3. 按「官网是否被引用或呈现」回填九格，并定档 `vis_1/2/3`
4. 全程不弹「请输入要查的词」给老板

## 推词规则（系统自动，可人工改后再跑）
来源优先级：项目已锁定词池 → 否则按域名/品牌/行业生成草稿词（各 1 个默认可跑通九格）→ 写入诊断本次实测词快照。
| 词类 | 用途 | 示例逻辑 |
|------|------|----------|
| 决策词 | 品类推荐 | 行业+品类「怎么选/推荐」 |
| 场景词 | 采购/使用场景 | 业务场景词 |
| 对比词 | 品牌对比 | 「A vs B」类，含本品官网对象 |
| ~~品牌词~~ | — | **禁止** |

## 产品步骤（嵌进 diagnosis 流程）
1. 评分台维度四区 / 发现前：一键「DeepGEO 查可见度」（默认在抓取后或进入评分时自动触发一次，可重跑）
2. 系统展示将要查的 3 词（只读摘要，高级可展开改词后重跑——**默认不要求老板操作**）
3. 调用 DeepGEO → 回填九格 + 证据链引用（底稿级可后补）
4. 自动写入 `vis_*` 建议分；人工仍可改档
5. 失败：保留「人工录入九格」兜底，并记跳过原因（会话失效等）

## 验收
- [ ] **不要再手动点九格格子**：默认模拟人工，自动走 DeepGEO 查豆包/DeepSeek/通义并回填（`runDeepgeoSession` / pending_proxy 等）
- [ ] 新诊断默认自动触发；**自动查成功后可少点确认**（结果直接写入九格+建议分，至多一次「采用结果」；禁止逐格手点）
- [ ] **无「请输入查询词」弹窗**；词仅决策/场景/对比，来自当前项目域名或词池
- [ ] 九格回填后维度四可算分；报告速览表一致
- [ ] **失败仍人工**：会话失效/查询失败 → 明确失败态 + 人工录入九格兜底，不卡死全流程
- [ ] client 不可触发查询；operator/lead 可

## 非目标（本轮）
- 不替代日常监测看板的 10 次/词 KPI 口径（那是 P1）
- 不要求老板每次提供「查什么」

## 协作
| 角色 | 动作 |
|------|------|
| 媒介运营 | DeepGEO 网页登录 + inclusionQuery 九格 JSON；调 applyVisGrid(deepgeo_web) |
| 后端 | 推词 API + applyVisGrid(deepgeo_web) 落库；Open API 可选 |
| 前端 | 默认按钮/自动触发、结果九格、失败人工兜底；无问词弹窗 |
| 产品 | 本说明；并入验收 v1.3 |
